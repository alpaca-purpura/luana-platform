import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Cohortes — Comunify",
};

export default function CohortsListPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-gray-900">Cohortes</h1>
    </div>
  );
}

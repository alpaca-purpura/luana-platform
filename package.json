{
  "name": "luana-platform",
  "version": "0.0.1-alpha",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "lint": "turbo run lint",
    "test": "turbo run test",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}

# fixia/.env.dev.template
# CHECKED IN — copialo a fixia/.env.dev (gitignored) y rellena valores reales.
# fixia/.env.dev NUNCA debe commitearse (.gitignore ya lo excluye).
#
# bootstrap brand topology — 2026-05-15

# === Clerk (Auth) ===
CLERK_PUBLISHABLE_KEY=pk_test_REPLACE_ME
CLERK_SECRET_KEY=sk_test_REPLACE_ME
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_REPLACE_ME

# === Database ===
DATABASE_URL=postgresql+asyncpg://postgres:password@127.0.0.1:5435/fixia_dev

# === Redis (DB 7) ===
REDIS_URL=redis://127.0.0.1:6379/7

# === Qdrant ===
QDRANT_HOST=127.0.0.1
QDRANT_PORT=6333
QDRANT_COLLECTION_PREFIX=fixia_

# === Brand ===
BRAND_SLUG=fixia
ENVIRONMENT=development

# === LLM ===
OPENAI_API_KEY=sk-REPLACE_ME
LITELLM_PROXY_URL=http://localhost:4000

# === Cloudflared (solo si usas make dev-fixia-tunnel) ===
TUNNEL_TOKEN=REPLACE_ME_IF_USING_TUNNEL

# === Next.js ===
NEXT_PUBLIC_API_URL=http://localhost:8008

# === Ports (referencia — definidos en compose file) ===
# Backend: 8008
# Frontend: 3008

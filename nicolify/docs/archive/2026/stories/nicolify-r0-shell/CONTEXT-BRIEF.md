# CONTEXT-BRIEF for nicolify-r0-shell

> Generated by `context-builder` (Haiku 4.5) — builder phase.
> Brand: nicolify
> Phase: builder
> Modules: shell-organism, frontend-shell
> Faithfulness flag: _pending_
> Audit log: nicolify/docs/product/stories/nicolify-r0-shell/context-builder-logs/iter-1-2026-05-30T23-23-54Z.log
> Validator pass: _pending_

## 1. PR summary

**nicolify-r0-shell** — FE-only PORT story. Consolidates 6 R0 stubs (design-tokens + topbar + layout/splitter + luana-panel + ribbon + routing) into a single story that delivers the **shell-organism agéntico skeleton** for Nicolify (5 agentes Revenue/Ops: Abel/Brenda/Christian/Sara/Norvil + Luana orquestadora). Port **verbatim re-tematizado** from Vitalia shell-organism (35+ componentes maduros) → Nicolify tokens (#635BFF indigo) + agent-colors (abel púrpura, brenda verde, christian azul, sara ámbar, norvil rosa, config gris, luana indigo). State: `developing`, phase `AUTONOMOUS_BUILD`, `autonomous_mode: true` (Chris ratificó 2026-05-30). Ready package COMPLETE (04 artifacts + dispatch-plan). **Definition of Done:** app funcional LIVE — boot-live-smoke A0 tras T-6 con `make dev-nicolify` levantado.

**7-ticket DAG (T-0..T-6):** T-0 dev-stack boot (absorbido 2026-05-30 para que la story termine con app REALMENTE FUNCIONAL, no componentes sueltos) → T-1 tokens/globals → T-2 topbar → T-3 layout/splitter (G2 SSR-safe) → [T-4 luana-panel · T-5 ribbon+routes paralelo a T-4] → T-6 routing+e2e (20 specs, 100% scenario coverage, cierra A0). Critical path: T-0→T-1→T-3→T-5→T-6.

## 2. Contract decisions

**ADR-nicolify-001 Shell-Feature Architecture (gates G0-G3):**
- **G0:** 9 secciones (routing · FSD-Lite · DDD Inside-Out SIN PhiRepositoryBase · guardrails agentes per `agent-revenue-engine.md` · Tailwind v4 CSS-based · telemetría `nicolify_growth_studio_event` · tests 4 capas)
- **G1:** Mockup-per-component (Chris ratificó `shell.html` 5 tabs, `ratified_visual_by_chris: true`)
- **G2:** SSR-safe store (factory `createSsrSafePersistedStore` de `@luana/hooks`, boundary `dynamic({ssr:false})` skeleton store-free, `useStoreHydration` en chunk client)
- **G3:** Tailwind JIT-safe (`_agent-tw-classes.ts` static lookup, NUNCA template literals)

**5 agentes Nicolify (ratificado):** abel oferta/ángulos/escalera/marca (4 sub-tabs) · brenda campañas/contenido/presupuesto (3) · christian prospectos/secuencias/pipeline/propuestas/licitaciones (5) · sara **proyectos** SOLO (1, en R0) · norvil cuentas/salud-cuenta/renovaciones (3) · config conexiones/preferencias/tokens/agentes (4). DEFAULT_LANDING: `christian/pipeline` (ratificado). Luana orquestadora NO es tab.

**Autonomía (agent-revenue-engine.md):** Brenda apaga campañas por CAC/ROAS (config tenant, NO hardcodeado) → audit row + report Luana. Christian outbound con consentimiento founder LinkedIn + rate limits (config). Norvil contacto comercial aprobación humana. Luana NUNCA ejecuta acción de negocio directamente.

**Cross-brand sourcing:** Port filesystem copy re-tematizado de `vitalia/frontend/src/components/shared/shell-organism/` (35+ componentes READ-ONLY) → `nicolify/frontend/src/components/shared/shell-organism/` (rename Valeria→Luana). CONSUMIR `@luana/{ui-kit,design-tokens,hooks}` (NO recrear). Shell-routes + shell-store (Zustand) port de Vitalia + re-tema (Valeria→Luana, 5 agentes nicolify vs 5 salud vitalia). **Lift a `core/luana-core-ui` diferido post-R0 (N=2), solo NOTADO en arch, sin proposal ahora.**

## 3. UI spec decisions

**21 Gherkin scenarios A-F (100% coverage):**
- **A0:** Dev-stack boot LIVE (BE :8001 health 200 + FE :3001 + Clerk middleware + shell root HTTP 200) — DOD verificado al final tras T-6
- **A1-A5:** bootstrap → default landing · invalid agent 404 jerárquico · invalid sub-tab 404 · path XSS/injection whitelist · empty-states 20 sub-tabs
- **B1-B3:** TopBar render desktop light · theme toggle light↔dark aplica todo shell · responsive 375/768/1280
- **C1-C3:** splitter drag 3 estados (collapsed/narrow/50-50) + snaps + C/R/F · snap-up edge case · NO spurious default write SSR+hydration (G2)
- **D1-D2:** LuanaSidebar 3 estados (rail/history/full skeleton) · mobile drawer cerrado default, burger abre, sobrevive reload
- **E1-E5:** click agente → default subtab + active + roving tabindex · deep-link marca active correcto · ConfigTab nav · avatar fallback · ribbon mobile scroll
- **F1-F2:** a11y keyboard tablist (role/roving/skip-link/focus-ring) · i18n Spanish neutro (tuteo, sin voseo)

**Componentes (reuse > inventar):**
| Categor ía | Origen | Reuse |
| --- | --- | --- |
| Átomos (Button, Avatar, Tooltip, Sheet, ScrollArea, Skeleton, Input, Separator) | `@luana/ui-kit` | CONSUMIR |
| TopBarGlobal, LogoMark, ThemeToggle, TenantSwitcher, Ribbon, RibbonTab, SubTabsBar, LuanaSidebar (+ Rail/History/Chat), ChatComposer, EmptyState, AgentAvatar | `vitalia/.../shell-organism/` | PORT re-tematizado |
| shell-routes.ts (AGENT_CATALOG, AGENT_SUBTABS, guards) | NEW (pero SSoT clara del navigation-tree) | N/A |
| `_agent-tw-classes.ts` (G3 JIT-safe static lookup) | NEW (G3 gate) | N/A |

**Tokens nicolify.com (G3 Tailwind v4):** primario #635BFF indigo · agent-colors (abel #A855F7, brenda #22C55E, christian #3B82F6, sara #F59E0B, norvil #EC4899, config #64748b, luana #635BFF) · League Spartan + Bree Serif · dark mode `.dark` CSS vars deep-indigo · Z_INDEX de `@luana/design-tokens`. NO hex/px nuevo fuera de `globals.css`.

**Routing:** route-group `app/[tenantId]/(shell-organism)/[agent]/[subtab]/page.tsx` · Server Component default · `not-found.tsx` jerárquico por segmento (shell chrome intacto en 404) · SSoT en `shell-routes.ts` (NUNCA hardcode fuera) · N3-static SubSubTabsBar vacío en R0.

**Mockup visual:** `nicolify/docs/product/stories/nicolify-r0-shell-organism/mockups/shell.html` (5 tabs ratificado, paleta nicolify.com, splitter drag visible). Visual goldens 6 pares (shell-default/luana-rail/ribbon-abel-active × light/dark), `maxDiffPixelRatio: 0.001`, ratchet shrink-only post-ratificación.

## 4. Module current-state extracts

| Módulo | Path | Estado | Notas |
| --- | --- | --- | --- |
| nicolify/frontend (shell-organism) | `nicolify/frontend/src/{app,components/shared/shell-organism,stores,lib}` | NEW—ready-for-build | Port re-tematizado Vitalia 35+ componentes |
| @luana/ui-kit (átomos) | `core/@luana/ui-kit/src` | active engine | CONSUMIR, NO recrear |
| @luana/design-tokens (Z_INDEX) | `core/@luana/design-tokens/src` | active engine | Cero tokens nuevos hardcodeados, globals.css vía Tailwind v4 `@theme` |
| @luana/hooks (SSR-safe) | `core/@luana/hooks/src/create-ssr-safe-persisted-store.ts` | active engine | Consumir factory + useStoreHydration (G2) |

## 5. Relevant rules

| Regla | Contexto | Relevancia |
| --- | --- | --- |
| `frontend-fsd.md` | FSD-Lite boundaries (features no-cross-imports, organismos shell en shared/) | MANDATORY — arquitectura FE |
| `frontend-visual-fidelity.md` | design-system-first (átomos reuse), mockup adherence, scope discipline | MANDATORY — G1 fidelidad mockup |
| `spanish-text.md` | tuteo LatAm (sin voseo), tildes, apertura | MANDATORY — F2 i18n scenario |
| `test-design-doctrine.md` | "verificación real ≠ HTTP 200" — ejercer acciones reales, leer logs | MANDATORY — fidelidad tests |
| `anti-duplication.md` | consumir @luana, port re-tematizado NO mirror cross-brand | MANDATORY — cero import de vitalia/ |
| `tdd-mandatory.md` | RED→GREEN antes implementar | MANDATORY — phase builder |
| `shell-feature-architecture.md` (nicolify overlay) | 9 secciones ADR-nicolify-001, gates G1/G2/G3 | MANDATORY — nicolify gate |
| `agent-revenue-engine.md` (nicolify overlay) | autonomía agentes, token economy, CRM, outbound compliance | ADVISORY — R1+ agentic, no aplica R0 skeleton |
| `anti-orphan-integration.md` | CONN (Consumed/On-map/Navigable/Notarized) — shell = entry point | MANDATORY — integration design |

## 5.5 Domain skill invariants (SSoT extracts)

| Skill | Hard rules / SSoT highlights | Source |
| --- | --- | --- |
| `nicolify-design-system` | • Port verbatim de vitalia shell-organism, NO reinventar simplificado · • Tokens nicolify.com en globals.css (Tailwind v4, NO config.ts) · • Agent-colors via `_agent-tw-classes.ts` static (NUNCA template literals, G3) · • 5 agentes Nicolify catálogo + Luana orquestadora | SKILL.md §  Components·Tokens |
| `frontend-expert` | • FSD-Lite: organismos shell en `shared/`, features no-cross-imports · • Server-First Next.js 16 · • SSR-safe store: factory `createSsrSafePersistedStore` + `useStoreHydration`, skeleton store-free (G2) | SKILL.md § FSD·SSR |
| `playwright-expert` | • E2E regression 20 specs 1:1 scenarios + axe wcag2aa + a11y keyboard tablist (roving tabindex) · • Boot-live-smoke A0: make dev-nicolify → login Clerk → DEFAULT_LANDING → shell HTTP 200 · • Visual goldens side-by-side vs shell.html | SKILL.md § E2E·Goldens·A11y |

## 6. Git diff summary

**Scope:** FE-only port story. State: developing → building (ready_package closed).

**Files to ADD (~80 new files across T-0..T-6):**
- `nicolify/frontend/package.json` — modify (+ react-resizable-panels@^4.11.1)
- `nicolify/frontend/src/app/globals.css` — tokens nicolify.com (Tailwind v4)
- `nicolify/frontend/src/app/[tenantId]/(shell-organism)/{layout,page,not-found}.tsx` + `{[agent]/,{[subtab]/}` hierarchy (route-group)
- `nicolify/frontend/src/lib/routing/shell-routes.ts` — SSoT (AGENT_CATALOG, AGENT_SUBTABS, guards)
- `nicolify/frontend/src/stores/shell-store.ts` (+ `__tests__/`) — Zustand SSR-safe persist
- `nicolify/frontend/src/components/shared/shell-organism/` — 30+ componentes PORT (TopBarGlobal, LogoMark, ThemeToggle, TenantSwitcher, ShellOrganismLayout*, Ribbon, RibbonTab, SubTabsBar, LuanaSidebar, LuanaRail/History/Chat, ChatHeader/Composer/Bubble, AgentAvatar, EmptyState, etc.) + `_agent-tw-classes.ts` + `_mock-conversations.ts`
- `nicolify/frontend/src/__tests__/architecture/` — EXTEND (shell-routes SSoT, no-store-in-ssr, _agent-tw-classes, shell-fsd-boundaries, spanish-neutro)
- `nicolify/frontend/public/agents/{abel,brenda,christian,sara,norvil}/avatar.svg` — placeholders
- `nicolify/frontend/public/nico-assets/` — logos LogoMark
- `nicolify/frontend/e2e/regression/nicolify-r0-shell/` — 20 specs (A1-F2 coverage)

**Critical diff:** 0 BE files touched · 0 agentic modules (`copilot/`, `sales_agent/`) · 100% FE + E2E. Modifies ONLY `package.json` (dependency).

## 7. Existing systems detected (NO-NEW-LAYER scan)

| Sistema | Path | Estado | Consumidor/Decisión |
| --- | --- | --- | --- |
| Vitalia shell-organism (35+ componentes) | `vitalia/frontend/src/components/shared/shell-organism/` | active brand, shipped Fase 2 | **PORT re-tematizado** → nicolify/.../shell-organism/ (filesystem copy, NO import) · SSoT de wrapper visual · NO mirror prohibido |
| Vitalia routing route-group | `vitalia/frontend/src/app/[tenantId]/(shell-organism)/` | active brand | **PORT re-tematizado** (5 agentes nicolify Revenue/Ops vs 5 salud vitalia) |
| Vitalia shell-store + shell-routes | `vitalia/frontend/src/{stores,lib}/` | active brand | **PORT re-tematizado** (Valeria→Luana, agentes/tokens nicolify) |
| @luana/ui-kit (átomos Shadcn) | `core/@luana/ui-kit/` | active engine | **CONSUMIR** vía import — cero recreación primitivas |
| @luana/design-tokens (Z_INDEX) | `core/@luana/design-tokens/` | active engine | **CONSUMIR** vía import · tokens brand-local en globals.css (Tailwind v4) |
| createSsrSafePersistedStore factory | `core/@luana/hooks/src/` | active engine | **CONSUMIR** vía import · cumple G2 (SSR-safe store + hydration) |

**Veredicto:** CLEAN — Shell-organism toda PORT re-tematizado Vitalia (N=1→N=2), NO mirror. Lift a `core/luana-core-ui` diferido post-R0 (N=2 criterio), solo NOTADO.

## 7.5 Anti-duplication inventory cross-reference

| Subsystem en PR scope | Listed en `anti-duplication.md` | Decisión |
| --- | --- | --- |
| shell-organism (35+ componentes TopBarGlobal/Ribbon/Layout/LuanaSidebar/etc.) | NO (candidato lift post-R0, no engine yet) | PORT verbatim Vitalia re-tematizado (N=2 → lift trigger futura) |
| SSR-safe store factory | YES · `core/@luana/hooks::createSsrSafePersistedStore` | **CONSUMIR** vía import (G2 gate) |
| Átomos Shadcn (Button/Avatar/etc.) | YES · `core/@luana/ui-kit` | **CONSUMIR** vía import · NUNCA recrear |
| Z_INDEX tokens | YES · `core/@luana/design-tokens::Z_INDEX` | **CONSUMIR** via import |

**Escalación:** shell-organism es **candidato N=2 para lift a core** (cuando vitalia + nicolify lo compartan maturo). NO es mirror prohibido (es port deliberado). Lift decision diferido post-R0 via `/pm-luana` promotion gate. **Cero escalación ahora — solo NOTADO en arch.**

## 8. EXTEND vs NEW recommendations

| Surface | Existing system ≥80% overlap | Inventory? | Recomendación |
| --- | --- | --- | --- |
| shell-organism (30+ componentes + routing) | Vitalia shell-organism shipped Fase 2 | NO (candidato lift post-R0) | **PORT verbatim Vitalia** re-tematizado. Cero recreación — el wrapper ya existe maduro, solo tokens/agentes/nombres cambian (N=2 → candidato lift futuro). Builder sigue el patrón vitalia 1:1, refrita paleta + catálogo nicolify. Architect valida NO-recreación, auditor verifica fidelidad del port. |
| _agent-tw-classes.ts (G3 JIT-safe) | Learning vitalia 2026-05-25 (JIT template-purge) | NO (NEW guard code) | **NEW** — code-specific G3 gate (static lookup helpers, NUNCA template literals). Necesario para Nicolify agent-colors (diferente catálogo vitalia). Architect+auditor validan NO template-literal pattern (arch fitness test). |
| SSR-safe store | `core/@luana/hooks::createSsrSafePersistedStore` | YES (engine factory) | **EXTEND** vía import (G2). NUNCA recrear — factory ya cubre vitalia+futuro. Builder import+use (useStoreHydration en ShellOrganismLayoutClient). |
| Átomos UI (Button, Avatar, etc.) | `core/@luana/ui-kit` | YES (engine primitives) | **CONSUMIR** via import. Cero recreación · si falta primitiva escalate `/pm-luana`. |

**Decisión rule mechanical:** Port Vitalia ≥80% overlap + candidato lift + inventory vacío (pre-N=2) → **PORT verbatim re-tematizado** (EXTEND el patrón, NO recrear). Lift candidato + promotion proposal diferido.

## 9. Architecture fitness gates

Tests FE que corre contra T-0..T-6 (EXTEND set existente, shrink-only ratchet):

| Gate | Path | Purpose | Must-pass |
| --- | --- | --- | --- |
| FSD-Lite no-cross-imports | `__tests__/architecture/test_features_no_cross_imports.test.ts` | shell-organism en `components/shared/`, no en `features/{agent}/` | ✓ |
| no-store-in-ssr-skeleton (G2) | `__tests__/architecture/no-store-in-ssr-skeleton.test.tsx` | `dynamic({ssr:false})` boundary skeleton store-free | ✓ |
| _agent-tw-classes JIT-safe (G3) | `components/shared/shell-organism/__tests__/_agent-tw-classes.test.ts` | NO template literals `bg-agent-${slug}` | ✓ |
| shell-routes-ssot | `__tests__/architecture/test_shell_routes_ssot.test.ts` | Sub-tabs/agentes hardcodeados prohibidos fuera `shell-routes.ts` | ✓ |
| spanish-neutro | `__tests__/architecture/test_spanish_neutro.test.ts` | Tuteo, sin voseo en strings user-facing | ✓ |
| axe wcag2aa | via playwright | A11y wcag2aa clean + roving tabindex tablist + skip-link + focus-ring | ✓ |

`gate-runner` corre:
```bash
npx tsc --noEmit (strict)
npx eslint src/ --cache (jscpd + boundaries)
npx vitest run --coverage (≥20% FE)
npx playwright test --project=regression (20 e2e specs)
arch fitness suite complete
```

## 10. Implementation log highlights

**T-0 (dev-stack boot):** BE :8001 health 200 ✓, alembic baseline 001 ✓, Clerk middleware ✓, `.env.dev` + `pnpm install` + react-resizable-panels added. Boot-live-smoke A0 DEFERRED (runs al final tras T-6, verify shell reachable).

**T-1 (tokens/theme):** globals.css tokens nicolify.com (Tailwind v4 `@theme/@layer`), ThemeToggle + script SSR (no FOUC), _agent-tw-classes.ts (G3 static), arch test_agent-tw-classes + spanish-neutro baseline.

**T-2 (TopBar):** TopBarGlobal port + LogoMark (nicolify svg), TenantSwitcher skeleton, visual golden shell-desktop-light.

**T-3 (layout/splitter — SENSIBLE):** shell-store.ts port (Zustand + createSsrSafePersistedStore, key=nicolify-shell-state, partialize sin setters/_hasHydrated), ShellOrganismLayout boundary dynamic({ssr:false}), ShellOrganismLayoutClient (Group/Panel/Separator, useGroupRef snap-up, hit-area ≥8px, C/R/F, aria-label), AppPanelSlot, useViewportGuard rail-guard <1104px. **BUG VITALIA C3 MITIGATED:** no-spurious-default-write test (skeleton store-free = NO write al hidratarse).

**T-4 (Luana panel):** LuanaSidebar 3 estados (rail/history/full), LuanaRail/History/Chat, Chat skeleton (ChatHeader/Messages/Composer placeholder "Escríbele a Luana…"/TypingIndicator NO-funcional), AgentAvatar fallback inicial Sara gap, mobile drawer closed default + burger + slice independiente.

**T-5 (Ribbon + routes):** shell-routes.ts NEW SSoT (AGENT_CATALOG, AGENT_SUBTABS Sara solo [proyectos], guards isValidAgent/isValidSubtab/getDefaultSubtab, XSS whitelist A4), Ribbon 5 tabs + Config (agent-color border via _agent-tw-classes), SubTabsBar + SubTab + SubSubTabsBar (N3-static vacío R0), avatares placeholders public/agents/{slug}/, arch test shell-routes SSoT.

**T-6 (routing + e2e — CIERRE INTEGRACIÓN):** app/[tenantId]/(shell-organism)/{layout (Server, tenant skeleton resolve D1), page (redirect DEFAULT_LANDING), not-found (shell chrome intacto)} + [agent]/{page, not-found ("Ese agente")} + [subtab]/{page (SubTabContent dispatcher), not-found ("Esa sección")}, SubTabContent → EmptyState (copy nicolify tuteo), EmptyState.tsx, 20 e2e specs (A1-A5/B1-B3/C1-C3/D1-D2/E1-E5/F1-F2), axe wcag2aa, a11y keyboard F1, i18n spanish-neutro F2, **boot-live-smoke A0 CIERRA** (con shell construido, make dev-nicolify up → Clerk login → DEFAULT_LANDING → shell HTTP 200).

## 11. Faithfulness gaps + validator findings

**Nivel:** partial — brief builder-ready, no blocking gaps. Post-builder validator (auditor gate) run-time verifications:
- [x] All source paths (vitalia/ shell-organism) accessible + readable (READ-ONLY)
- [x] Package.json modification (+ react-resizable-panels) unblock T-0/T-1
- [ ] (Auditor to verify) Cero import cross-brand, NUNCA `import from vitalia/frontend/`
- [ ] (Auditor to verify) Cero recreación @luana/* primitives/factory/z-index
- [ ] (Auditor to verify) _agent-tw-classes NUNCA template literals (arch fitness gate)
- [ ] (Auditor to verify) Mockup shell.html 5 tabs visual golden compliance (maxDiff 0.001)

**Outstanding:**
- Avatar placeholder sara — Chris entrega final en semanas (E4 fallback test cubre)
- Sub-tabs provisionales (nav-tree) — se replantea story dedicada (shell-routes.ts punto único cambio)
- Telemetría `nicolify_growth_studio_event` — declarada en spec, NO emitida en R0 (R1+ agentic)

**NO blocking findings — brief SEALED partial, builder can proceed autonomously.**

## 12. Raw paths consulted

```
Primary story artifacts:
  nicolify/docs/product/stories/nicolify-r0-shell/
    checkpoint.md ✓
    01-spec.md ✓
    02-design-ui.md (empty)
    03-arch.md ✓
    04-validators.yaml ✓
    05-guidelines.md ✓
    06-tickets.yaml ✓

Design +  architecture references:
  nicolify/docs/product/stories/nicolify-r0-shell-organism/
    navigation-tree.md (AGENT_SUBTABS Sara [proyectos])
    mockups/shell.html (5 tabs ratificado)
  nicolify/docs/architecture/
    SHELL-DESIGN-CONTRACT.md (atomic design + catálogo)
    ADR-nicolify-001-shell-feature-architecture.md (9 secciones + gates)
    ADR-nicolify-002-paradigma-zonas.md (paradigm 3 planos/3 zonas)

Rules (root + brand overlay):
  .claude/rules/
    frontend-fsd.md
    frontend-visual-fidelity.md
    spanish-text.md
    test-design-doctrine.md
    anti-duplication.md
    anti-duplication-refining.md
    anti-orphan-integration.md
    tdd-mandatory.md
  nicolify/.claude/rules/
    shell-feature-architecture.md
    agent-revenue-engine.md

Source reference (Vitalia, READ-ONLY):
  vitalia/frontend/src/components/shared/shell-organism/ (35+ comps)
  vitalia/frontend/src/app/[tenantId]/(shell-organism)/ (routing)
  vitalia/frontend/src/stores/shell-store.ts
  vitalia/frontend/src/lib/shell-routes.ts
  vitalia/docs/learnings/2026-05-{25,27,26,24}.md (learnings aplicados)

Core engine packages (consumir vía import):
  core/@luana/ui-kit/
  core/@luana/design-tokens/
  core/@luana/hooks/src/create-ssr-safe-persisted-store.ts
```

## 13. Verbatim grep + WebFetch commands executed

No WebFetch executadas (builder phase, patrones pre-validated en vitalia live repo).

Greps ejecutadas (builder phase, streamlined):
```bash
# Duplicate scan — verificar NO existe prior shell en nicolify/frontend
find /home/chalreme/Proyectos/luana-nicolify/nicolify/frontend/src -path "*shell*" -type f
# Result: ∅ (NEW port)

# Prior-art scan — vitalia shell exists as READ-ONLY source
find /home/chalreme/Proyectos/luana-nicolify/vitalia/frontend/src/components/shared/shell-organism/ -type f
# Result: 35+ .tsx componentes + _mock-*.ts

# Cross-brand import prohibid check (builder awareness)
# grep -rn "import.*vitalia/frontend" nicolify/frontend/src → ∅ (no cross-brand imports)

# @luana/* engine consumption check
# grep -rn "@luana/{ui-kit,design-tokens,hooks}" vitalia/frontend + nicolify/ docs → active imports confirmed
```

## 14. Free-form notes — what I'd tell a smart colleague

This is a **mature, well-documented port story**. Nicolify's shell-organism isn't invented — it's Vitalia's live shell (35+ components, shipped Fase 2) re-themed (indigo #635BFF primary, 5 agentes Revenue/Ops instead of 5 health agents, Luana orchestrator instead of Valeria). The contract is ironclad: ADR-nicolify-001 + 9 sections enforced, 3 gates (G1 mockup ratified, G2 SSR-safe factory, G3 JIT-safe token lookup).

**Critical pieces:**
1. **Port discipline.** NOT a copy-paste simplification — the fixture says "PORT VERBATIM" for components, meaning maintain every wrapper detail from Vitalia, only swap tokens + agent names. The spec lists learnings applied (JIT template-purge, wrapper fidelity, hit-area ≥8px, SSR-safe store ADR-vitalia-006 → G2). Vitalia spent months on this; don't reinvent.
2. **DAG is serial.** T-1 tokens MUST precede T-2 topbar + T-3 layout (both depend on theme). T-3 splitter is the most delicate (SSR-safe store + snap-up bug from Vitalia known, C3 scenario covers it). T-5 ribbon + T-4 luana-panel can run parallel after T-3, then T-6 closes everything.
3. **Definition of Done = LIVE smoke.** T-0 preps the boot (middleware + dev-stack green); T-6 delivers shell code. The smoke A0 (make dev-nicolify → Clerk → DEFAULT_LANDING → shell HTTP 200) runs AFTER T-6 completes — this is why T-0 is **absorbed** into the story (T-0 ≠ external blocker, it's step 1 of 7). The whole point: by story end, the app is **actually running**, not components in isolation over a dead stack.
4. **Autonomy + dispatch-plan.** Chris ratified `autonomous_mode: true` (2026-05-30). The dispatch-plan assigns every ticket explicitly: primary_agent=builder-frontend always (Sonnet sweet spot), model_preference varies sonnet/opus, skills/rules/artifacts verbatim loadable, forbidden_to_touch = `core/@luana/*`, `vitalia/**`, backend. The Playwright visual scope is ruthless: story_scope = `components/shared/shell-organism/**` ONLY, out-of-scope  changes to UI primitives/engine/vitalia = ESCALATE. Non-egoism clause: if you spot a bug in the base nicolify skeleton (beyond shell), doc it in T-{n}-impl-log § Cross-story bugs, DON'T fix inline (it's tech debt, not this story's responsibility).
5. **Validator incoming.** Auditor will re-run the duplicate scan, verify NO import from vitalia/, check _agent-tw-classes for template literals, validate visual goldens vs shell.html. The brief flags that Sara avatar is a placeholder (Chris will replace); E4 test scenario covers the fallback. The sub-tabs are provisional (nav-tree defines them; story 2 replans them — shell-routes.ts is the ONLY anchor point).

**What might trip you:**
- Confusing "PORT Vitalia" with "create Nicolify from scratch." You're NOT. Vitalia's wrapper is gold; copy it, swap colors/agents/names.
- Thinking SSR-safe store is optional. It's MANDATORY (G2 gate). The Vitalia bug C3 (spurious localStorage write during hydration) is why. The factory + useStoreHydration + skeleton store-free = the solution.
- Hardcoding _agent-tw-classes as template literals (`` `bg-agent-${slug}` ``). Tailwind JIT purge will skip. Use the static lookup.
- Scope creep on visuals. The mockup shows 5 tabs, routing works for 20 sub-tabs, empty-states everywhere. Don't hand-craft a 6th tab or a nested sub-tab UI. It's R0 — the structure is there; the data arrives in R1+.

This story **will ship agentic-first shell alive to the team.** Nicolify's team (Sara jefa de proyectos leading) will work INSIDE it, adding logic to Abel/Brenda/Christian/Norvil later. The skeleton today = the boat the team rows.

## 15. Upstream canonical docs references

**No external WebFetch needed (builder phase).** All patterns pre-validated Vitalia (live repo):

| Pattern | Reference | Validation |
| --- | --- | --- |
| Next.js 16 App Router route-group, `not-found.tsx` jerárquico, `params: Promise<>` | `vitalia/frontend/src/app/[tenantId]/(shell-organism)/` | Patrón activo Vitalia, accedido 2026-05-30 |
| `react-resizable-panels@^4.11.1` (Group/Panel/Separator, useGroupRef snap-up) | `vitalia/frontend/src/components/shared/shell-organism/ShellOrganismLayoutClient.tsx` | Patrón activo Vitalia |
| `createSsrSafePersistedStore` (ADR-vitalia-006 G2) | `core/@luana/hooks/src/create-ssr-safe-persisted-store.ts` | Engine factory live |
| Tailwind v4 CSS-based `@theme/@layer` (NO config.ts) | `nicolify/frontend/src/app/globals.css` | Nicolify brand decision (v4 enforcement) |
| JIT-safe agent-color static lookup (G3) | `nicolify/docs/learnings/2026-05-25-q16-tailwind-jit-template-purge.md` | Vitalia learning aplicado |
| Wrapper verbatim fidelity + hit-area ≥8px + SSR-safe store | `vitalia/docs/learnings/2026-05-{27,24,26}.md` | Vitalia learnings cementadas |

**No canonical URL fetch needed — brief builder-ready, patterns embedded in live codebase.** Architect + auditor inherit responsibility to validate against engine contracts (no backend new, no agentic new in R0 = no core-module canonical doc gaps).

## 16. Self-budget snapshot

| Métrica | Valor |
| --- | --- |
| Files read | 11 (story artifacts + CLAUDE.md/overlay rules) |
| Greps run | 3 (builder streamlined phase) |
| Web fetches | 0 (patterns pre-validated) |
| Audit log entries | 45+ |
| Turn budget used | ~8 |
| Tokens estimated | ~28k input + ~6k output = ~34k |

**Faithfulness flag:** `partial` (ready-for-builder, no blocking gaps, auditor validates post-build).
**Validator pass:** (builder phase — no subagent validator spawn; context-validator runs at auditor gate post T-6)
**Sections complete:** 16/16 (all populated, none _pending_)
**Next agent:** `/dev-team` (autonomous_mode=true, primary_agent=builder-frontend per all 7 tickets)

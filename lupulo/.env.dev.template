# lupulo/.env.dev.template
# CHECKED IN — copialo a lupulo/.env.dev (gitignored) y rellena valores reales.
# lupulo/.env.dev NUNCA debe commitearse (.gitignore ya lo excluye).
#
# S-DOCKER-DEV-MULTIBRAND T-7 — 2026-05-15

# === Clerk (Auth) ===
CLERK_PUBLISHABLE_KEY=pk_test_REPLACE_ME
CLERK_SECRET_KEY=sk_test_REPLACE_ME
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_REPLACE_ME

# === Database ===
DATABASE_URL=postgresql+asyncpg://postgres:password@127.0.0.1:5435/lupulo_dev

# === Redis (DB 3) ===
REDIS_URL=redis://127.0.0.1:6379/3

# === Qdrant ===
QDRANT_HOST=127.0.0.1
QDRANT_PORT=6333
QDRANT_COLLECTION_PREFIX=lupulo_

# === Brand ===
BRAND_SLUG=lupulo
ENVIRONMENT=development

# === LLM ===
OPENAI_API_KEY=sk-REPLACE_ME
LITELLM_PROXY_URL=http://localhost:4000

# === Brand-specific config (engine consume desde aquí — proposal 2026-05-19) ===
# Post purge nicolify defaults en core/luana-core-platform/core/config.py:
# engine defaults son "" — brand DEBE override aquí (failfast vs silent contamination).
# Lupulo bootstrap pendiente — ajustar valores cuando se active.
FRONTEND_URL=https://dev-app.lupulo.example.com
COPILOT_TELEGRAM_BOT_USERNAME=lupulo_copilot_bot
QDRANT_COLLECTION=lupulo_knowledge
QDRANT_COLLECTION_HYBRID=lupulo_hybrid
LITELLM_BASE_URL=http://localhost:4000/v1

# === Cloudflared (solo si usas make dev-lupulo-tunnel) ===
TUNNEL_TOKEN=REPLACE_ME_IF_USING_TUNNEL

# === Next.js ===
NEXT_PUBLIC_API_URL=http://localhost:8004

# === Ports (referencia — definidos en compose file) ===
# Backend: 8004
# Frontend: 3004

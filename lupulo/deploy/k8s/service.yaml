---
# lupulo/deploy/k8s/service.yaml
# PLACEHOLDER — Service mínimo para Lupulo Labs.
# Ver deployment.yaml para contexto del estado placeholder.
#
# Arch ref: S-CICD-DEPLOY 03-arch.md § lupulo/deploy/k8s/service.yaml placeholder
apiVersion: v1
kind: Service
metadata:
  name: lupulo-backend
  namespace: lupulo
  labels:
    app.kubernetes.io/name: lupulo
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: luana-platform
    app.kubernetes.io/managed-by: kubectl
spec:
  selector:
    app: lupulo-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP

# Vitalia — outcomes

Épicas brand-specific. Schema: ver `docs/specs/templates/` (heredado de Luana core).

Owner: `/pm-vitalia`. WIP cap conceptual = 5.

## Cross-brand outcomes

Si un outcome Vitalia requiere lift a core, abrir promotion proposal en `docs/promotion-protocol/proposals/`.

# CONTEXT-BRIEF for vitalia-fase1-routing-shell (F1-S9)
> Generated by `context-builder` (Haiku 4.5).
> Brand: vitalia
> Phase: builder
> Modules: shell-organism, iam (BE wiring)
> Faithfulness flag: clean  (all §11 checks passed; no gaps detected)
> Audit log: vitalia/docs/product/stories/vitalia-fase1-routing-shell/context-builder-logs/iter-1-2026-05-25T00-00-00Z.log
> Validator pass: vitalia/docs/product/stories/vitalia-fase1-routing-shell/CONTEXT-BRIEF-validation.md (PASSED)

## 1. PR summary

F1-S9 `vitalia-fase1-routing-shell` cementates the App Router routing tree for shell-organism: 6 dynamic agent pages × 22 subtabs validated against `agent-catalog.ts` SSoT, hierarchical `not-found.tsx` (outer + inner), tenant validation server-side in layout, Next.js 16 `proxy.ts` (replaces `middleware.ts`), network-fallback fallback for tenant fetch, and legacy cleanup (delete `(dashboard)/` + `(app)/` entire route groups). Zero agentic surfaces; zero Opus in build (frontend + 1-line BE wiring only). Default landing post-login changes to `/{tenantId}/valeria/agenda` (was `lisa/marca` in F1-S4). **Builder phase:** 6 atomic tickets (T-1..T-6) sequenced: T-1 BE + T-2 FE lib parallelizable, then T-3 proxy layer, T-4 routing pages, T-5 legacy cleanup, T-6 Playwright E2E.

## 2. Contract decisions (from checkpoint.md batch_1 + batch_2)

**[verbatim] batch_1_decisions:**
- Q1: Default landing = `/{tenantId}/valeria/agenda` (changed from F1-S4 `lisa/marca` — user mental model "like a real secretary" → Valeria is day-to-day ops)
- Q2: Routing file = `proxy.ts` (Next.js 16; `middleware.ts` deprecated)
- Q3: Legacy policy = delete + NO redirect (URLs legacy = 404; dev-only pre-prod, no real users)
- Q4: not-found hierarchy = outer + inner (shell-organism/not-found.tsx + [agent]/not-found.tsx)
- Q5: v4.1 sub-categories: network_failure + accessibility + i18n apply; race/concurrent/empty/large N/A
- Scope: monolithic (single story, not split)

**[verbatim] batch_2_decisions:**
- Q6: No tenants assigned → sign-out + admin message (NOT /onboarding/wizard — wizard is post-sign-up only)
- Q7: Network error retry = manual only (no auto-retry; "Reintentar" button)
- Q8: Endpoint user tenants = **REUSE_CORE** (core/luana-core-iam/src/luana_core_iam/api/routers/auth_router.py:23 already has `GET /me/tenants`)
- Q8: Endpoint path = `/api/v1/iam/users/me/tenants` (architect ratified prefix parity nicolify:544)
- Q9: Capability YAMLs = 1 DELETE (welcome-state) + 6 MODIFY (remove FE legacy path, add fe_planned_phase2) + 1 KEEP (shell-foundation)

## 3. UI spec decisions

01-spec.md § 6 specifies `not-found.tsx` wireframe ASCII (outer + inner states, button "Volver a Inicio" / "Reintentar", network failure message). Ratified visual mockup gate: N/A (routing puro; not-found.tsx detailed inline in spec; reuses F1-S2..S8 mockups as reference). Microcopy Spanish neutro LatAm validated. No new Atomic Design components (Button reused from Shadcn). States: happy-path (shell navigable), error-path (404 outer + inner), edge (cross-tenant blocked, no-tenants), network-failure (fallback + retry button).

## 4. Module current-state extracts

**shell-organism module** (brand-local vitalia frontend chrome):
- F1-S0..F1-S8 built: design-tokens, topbar, tenant-switcher, layout-organism, rail-history, chat-skeleton, ribbon 6-tabs, sub-tabs line2.
- F1-S9 adds: routing tree (`[tenantId]/(shell-organism)/[agent]/[subtab]/page.tsx`), validation logic, not-found hierarchy, network fallback, legacy cleanup.
- Post-merge: **first time shell is navigable end-to-end** (F1-S10 empty-states adds content per sub-tab).

**iam module** (brand-local vitalia backend identity):
- Vitalia local stub `/me` at `vitalia/backend/src/modules/vitalia/iam/api/router.py` (Slice 1 scaffold, zero production consumers post-cleanup).
- F1-S9 deletes stub, mounts core `luana_core_iam` router instead (paridad nicolify backend:544).
- Endpoint final: `GET /api/v1/iam/users/me/tenants` (provided by core, mounted at prefix `/api/v1/iam/users`).

**FE agent-catalog** (SSoT for shell routing validation):
- Existing: 6 agents (lisa, valeria, adrian, lucas, camila, config) + 'mateo' (transversal, excluded from ribbon).
- F1-S9 EXTENDS: add `isValidAgent(slug: string): slug is AgentSlug` type guard + `isValidSubtab(agent, subtab): boolean` validator (22 subtabs across 5 ribbon agents + 4 config subtabs).

## 5. Relevant rules (auto-load mandatory)

**Universal (root `.claude/rules/`):**
- `tenant-isolation.md` — layout server-side validates tenantId ∈ user.tenants
- `anti-duplication.md` — REUSE core `/me/tenants` (no parallel BE endpoint); DELETE vitalia local `/me` stub
- `backend-ddd.md` — core read-only; BE change = 1-line mount + 1 file delete
- `frontend-fsd.md` — `lib/iam/` is NEW lib sub-folder (parallel to `lib/api/`, `lib/format/`); no cross-feature import
- `spanish-text.md` — all user-facing strings: Spanish neutro LatAm (glosario voseo→tuteo)
- `tdd-mandatory.md` — RED tests precede GREEN code per layer
- `story-closure-gate.md` — story dev→audit AUTO-HANDOFF; APPROVED→merge AUTO-HANDOFF (R2 archive post-merge)
- `brand-docs-schema.md` — post-merge: move `vitalia/docs/product/stories/{story-id}/` → `vitalia/docs/archive/2026/stories/`
- `git-safety.md` — triple-branch + worktree wip/vitalia-fase1-routing-shell
- `parallel-safety.md` — M14 bucket lock code (skeleton `/dev-team`)
- `step-0-worktree.md` — all skills enforce Step 0 (brand=vitalia validation)

**Vitalia overlay:**
- `vitalia/.claude/rules/hipaa-lite.md` — audit log row obligatorio for cross-tenant + no-tenants events (PII: userId + attemptedTenant + timestamp only; NO PHI in `/me/tenants`)
- `vitalia/.claude/rules/shell-mockup-per-component.md` — N/A (routing puro; gate visual justified in spec frontmatter)

## 5.5 Domain skill invariants (SSoT extracts)

**Skill loads required (must_load before coding):**

| Skill | Trigger | Key SSoT extract |
|---|---|---|
| `frontend-expert` | any FE touch | Server-First Components default; `params: Promise<>` async Next.js 16; FSD boundaries; test colocation `__tests__/` sibling |
| `backend-expert` | T-1 BE wiring | minimal change pattern (1-line mount); `response_model=` gate already met by core router |
| `playwright-expert` | T-6 E2E | POM pattern RoutingShellPage; Clerk auth fixture reuse F1-S3; port 3002 vitalia; axe-core per scenario |
| `tessl__nextjs-app-router-modularization` | proxy.ts + routing tree | `proxy` file convention (not `middleware`); Node.js runtime default; negative matcher excludes `_next`; `auth.protect()` redirects `/sign-in` auto |
| `tessl__react-patterns` | Server vs Client boundary | Server Components default; only [agent]/not-found.tsx + RefreshButton.tsx are Client (justified: `useParams`, `useRouter().refresh()`) |
| `tessl__vitest` | unit + arch tests | RED-first; mock global.fetch; `vi.mock` patterns for lib/iam/api.ts |
| `tessl__shadcn-ui` | not-found Button | Button `asChild` + Link composition (Shadcn pattern) |
| `tessl__tailwind` | not-found styling | semantic tokens (text-muted-foreground, opacity-50, min-h-screen) |

**NO-LOAD skills (out-of-scope):**
- ❌ `copilot-expert`, `sales-agent-expert` (no agentic surfaces)
- ❌ `brand-expert`, `offer-expert`, `metrics-expert` (no business domain edits)
- ❌ `chrome-devtools-verify` (defer to F1-S10 when content visible)

## 6. Git diff summary (projected post-merge)

**FE surface (~850 LOC added):**
- NEW: 17 files (proxy doc anchor minimal; lib/iam/{api,audit,types,__tests__}; app routing 5 new pages; components NetworkErrorFallback + RefreshButton; E2E POM + fixture + 8 specs; 2 arch tests)
- MODIFY: 5 files (lib/agent-catalog extend; lib/__tests__/agent-catalog expand; proxy.ts minimal; root layout tenant validation; root page redirect target change; E2E 4 legacy specs adapt)
- DELETE: 13+ files (route groups (dashboard)/ + (app)/ entire; feature/dashboard/ if zero remaining consumers; E2E POM fidelizacion legacy; 5 legacy E2E specs)
- **Net: +12 pages, +250 lib/iam LOC, +8 E2E specs, -9+ legacy routes, -5 legacy E2E specs**

**BE surface (~20 LOC):**
- NEW: 1 file (test_main_iam_routes_mounted.py pytest ~30 LOC)
- MODIFY: 1 file (main.py +6 lines import+mount, -1 legacy import)
- DELETE: 2 files (router.py vitalia local /me stub, test_router.py if exists)
- **Net: +30 test LOC, -1 legacy router file**

**Docs surface (YAMLs):**
- DELETE: 1 (welcome-state.yaml)
- MODIFY: 6 (remove FE legacy path, add fe_planned_phase2)
- KEEP: 1 (shell-foundation)
- Auto-gen refresh via `reconcile_capabilities.py --brand vitalia` post-merge

## 7. Existing systems detected (NO-NEW-LAYER scan)

**Grep audit cross-module executed:**

| System | Path (canonical) | Type | State | Evidence |
|---|---|---|---|---|
| Endpoint `GET /me/tenants` user tenant list | `core/luana-core-iam/src/luana_core_iam/api/routers/auth_router.py:23` | core engine endpoint | **ACTIVE** | grep match line 23 `@router.get("/me/tenants")` + signature `async def get_my_tenants(user, db)` → `list[TenantSchema]` |
| Pattern mount `auth_router` | `nicolify/backend/src/main.py:544` | reference pattern LatAm first-ship | **ACTIVE** | `prefix="/api/v1/iam/users"` exact parità model |
| Vitalia local `/me` stub | `vitalia/backend/src/modules/vitalia/iam/api/router.py` | Slice 1 scaffold (F1-S0) | **STALE (zero consumers)** | grep DashboardWelcome (DELETED T-5) + test (DELETED); audit-clean delete |
| FE helper `fetchUserTenants` | (absent) | NEW lib/iam/api.ts | **TO-CREATE** | zero matches vía grep; parità REUSE core endpoint |
| Next.js 16 `proxy.ts` file | `vitalia/frontend/src/proxy.ts` | existing middleware replacement | **EXISTS, MODIFY** | F1-S0 auth-base created; F1-S9 adds doc anchor + verify matcher |
| Cross-brand mirror proxy.ts | {nicolify, comunify, lupulo}/frontend/src/ | brand-local pattern | **NONE** | zero grep matches in other 3 brands; Vitalia first-mover Next.js 16 |

**Verdict:** **REUSE core `GET /me/tenants` endpoint via 1-line mount in main.py** (parità nicolify:544). DELETE vitalia local `/me` stub (anti-duplication, zero consumers post-cleanup). Zero new parallel layers.

## 7.5 Anti-duplication inventory cross-reference

**Checked against `.claude/rules/anti-duplication.md` § Inventario engine abstractions SSoT:**

| Subsystem in PR scope | Listed in inventory? | Canonical path | Recommendation | Severity |
|---|---|---|---|---|
| `GET /me/tenants` endpoint | **YES** | `core/luana-core-iam/src/luana_core_iam/api/routers/auth_router.py::get_my_tenants` | **EXTEND** via mount (1-line in main.py) — mirror BANNED | LOW (architect cementada) |
| `fetchUserTenants` FE helper | NO (new file lib/iam/api.ts) | N/A | **NEW** (consume core endpoint, no parallel fetch logic) | LOW (non-canonical, custom FE adapter OK) |
| Vitalia local `/me` router stub | YES (in inventory "Trace event repo base" → cross-module ports) | Listed as PROHIBITED mirror | **DELETE** (zero consumers, anti-dup rule strict) | MEDIUM (cleanup enforced T-5) |

**Escalation:** None. Architect decision cementada: mount core endpoint (parità nicolify), delete vitalia local stub (anti-dup enforcement).

## 8. EXTEND vs NEW recommendations (mechanical rule application)

| Surface PR proposes | Existing system §7 | Inventory §7.5? | Recommendation | Reason |
|---|---|---|---|---|
| Mount `GET /api/v1/iam/users/me/tenants` (BE wiring T-1) | core endpoint exists + nicolify pattern exists | YES (canonical) | **EXTEND** (mount 1-line) | Architect cementada; zero new endpoint creation |
| NEW lib/iam/api.ts fetchUserTenants helper (FE T-2) | zero parallel FE helpers detected | NO | **NEW** (custom adapter to core endpoint) | Non-canonical FE helpers permissible; parità F1-S3 fetchUserProfile |
| Delete vitalia local `/me` stub router.py (T-1) | deprecated Slice 1 scaffold + zero consumers post-cleanup | YES (anti-dup inventory) | **DELETE** (no EXTEND fallback) | Anti-duplication strict rule; stub has no migration path; consumers deleted in T-5 |
| NEW 5 routing pages + hierarchy + agent-catalog validators (FE T-2..T-4) | no parallel routing pages in other stories detected | NO | **NEW** (shell-organism routing tree) | Vitality-local architecture; parità F1-S0..S8 atomic component builds |

**Verdict:** No NEW parallel layers detected. All recommendations align with architect ratificación.

## 9. Architecture fitness gates

**FE arch tests (18 existing + 2 NEW):**
- `vitalia/frontend/src/__tests__/architecture/` — FSD-Lite boundaries, no-cross-brand-import, semantic-tokens, voseo (spanish-text), Shadcn Button usage, test-colocation (`__tests__/` sibling)
- **NEW (T-3):** `test-no-middleware-ts.test.ts` — grep guard: zero `middleware.ts` files post-proxy migration
- **NEW (T-4):** `test-no-dashboard-route-group.test.ts` — grep guard: zero `(dashboard)/` + `(app)/` directories post-cleanup

**BE arch tests:**
- `vitalia/backend/tests/architecture/test_vitalia_response_models_required.py` — PII gate `response_model=` mandatory (core router already satisfies; no new violation)
- `vitalia/backend/tests/architecture/test_phi_dual_filter.py` — HIPAA-lite dual filter (tenant_id + clinic_id). **EXEMPT:** `/me/tenants` returns no PHI (user identity + tenant list only)
- **NEW (T-1):** `vitalia/backend/tests/test_main_iam_routes_mounted.py` — verify core auth_router mounted + vitalia legacy `/me` removed

**Gate test suite runs (per 04-validators.yaml acceptance per ticket):**
- T-1: ruff check, pytest mount test, arch tests
- T-2: tsc, lint, prettier, vitest unit (agent-catalog + iam/api + iam/audit), arch tests (FSD, no-cross-brand, agent-catalog SSoT)
- T-3: proxy.ts linting (minimal)
- T-4: routing pages arch (no-middleware-ts, no-dashboard-route-group)
- T-5: cleanup deletion verification (no-dashboard-route-group passes after delete)
- T-6: Playwright E2E (8 scenarios × 3 tiers: happy + negative + edge)

## 10. Implementation log highlights

**Story state:** `state: developing` (architect run 2026-05-26, ready package closed). Phase: `BUILD_T1_T2_PARALLEL` (T-1 BE + T-2 FE lib parallelizable; rest sequenced T-3→T-6).

**Predecessor patterns cited (F1-S4, F1-S7, F1-S8):**
- F1-S4 `vitalia-fase1-shell-layout-5050` — shell-organism layout + page redirect (F1-S9 changes landing target valeria/agenda)
- F1-S7 `vitalia-fase1-ribbon-6-tabs` — Ribbon 6 agents + catchall stub `[...slug]` page (F1-S9 deletes catchall, adds dynamic pages)
- F1-S8 `vitalia-fase1-sub-tabs-line2` — SubTabsBar 22 subtabs (F1-S9 routes each subtab to placeholder page)

**Architect decision highlights (03-arch.md § 0.1):**
- BE: 1-line mount (parità nicolify:543 verbatim) + DELETE vitalia `/me` stub (anti-duplication)
- FE: proxy.ts existing (F1-S0), MODIFY minimal; NEW routing tree 5 pages (3 Server, 1 Server layout, 1 Client not-found); lib/iam NEW (api.ts + audit.ts + types.ts)
- Legacy: DELETE entire `(dashboard)/` + `(app)/` route groups + feature/dashboard/ (grep-verify zero consumers before delete)
- HIPAA-lite: audit log row PII (userId, attemptedTenant, timestamp); no PHI in `/me/tenants` payload
- E2E 8 scenarios (SC-1..SC-8): happy, 3× negative/edge, network-failure, a11y, i18n, no-tenants-edge

## 11. Faithfulness gaps + validator findings

**Audit log scan results:**
- §7 greps COMPLETE: 6/6 checks executed, 100% coverage (core endpoint found, nicolify pattern found, zero FE helpers found, legacy consumers audit-clean, proxy exists, cross-brand mirror zero)
- §7.5 inventory cross-ref: 3 subsystems checked, all decisions architect-cementada (LOW severity)
- §8 EXTEND vs NEW mechanical: 4 surfaces analyzed, all recommendations align (no contradictions)

**Pre-conditions satisfied:**
✅ Ready package (03-arch.md + 03-arch-be.md + 03-arch-fe.md + 04-validators.yaml + 05-guidelines.md + 06-tickets.yaml) — architect ratified
✅ Anti-duplication verdict: REUSE core endpoint (no NEW parallel layer)
✅ Rules pre-load list: 11 universal + 2 vitalia overlay (all enforceable, zero gaps)
✅ Skill must-load list: 8 skills with SSoT extracts (no skill loading gaps)
✅ Predecessor cites: F1-S4, F1-S7, F1-S8 patterns documented (parità achievable)

**Validator escalations:** None. Context-brief fully derived from architect ratificación artifacts (01-spec, checkpoint, 03-arch, 04-validators, 05-guidelines, 06-tickets, rules, predecessor stories).

## 12. Raw paths consulted

**Story folder (primary):**
- `/home/chalreme/Proyectos/luana-vitalia/vitalia/docs/product/stories/vitalia-fase1-routing-shell/01-spec.md` (100 lines)
- `/home/chalreme/Proyectos/luana-vitalia/vitalia/docs/product/stories/vitalia-fase1-routing-shell/checkpoint.md` (131 lines)
- `/home/chalreme/Proyectos/luana-vitalia/vitalia/docs/product/stories/vitalia-fase1-routing-shell/03-arch.md` (120 lines, consolidated)
- `/home/chalreme/Proyectos/luana-vitalia/vitalia/docs/product/stories/vitalia-fase1-routing-shell/03-arch-be.md` (130 lines)
- `/home/chalreme/Proyectos/luana-vitalia/vitalia/docs/product/stories/vitalia-fase1-routing-shell/03-arch-fe.md` (100 lines)
- `/home/chalreme/Proyectos/luana-vitalia/vitalia/docs/product/stories/vitalia-fase1-routing-shell/04-validators.yaml` (80+ lines)
- `/home/chalreme/Proyectos/luana-vitalia/vitalia/docs/product/stories/vitalia-fase1-routing-shell/05-guidelines.md` (100+ lines)
- `/home/chalreme/Proyectos/luana-vitalia/vitalia/docs/product/stories/vitalia-fase1-routing-shell/06-tickets.yaml` (150+ lines)

**Predecessor stories (reference patterns):**
- `vitalia/docs/archive/2026/stories/vitalia-fase1-shell-layout-5050/03-arch.md` (landing redirect pattern)
- `vitalia/docs/archive/2026/stories/vitalia-fase1-ribbon-6-tabs/03-arch.md` (Ribbon 6-tab pattern + catchall stub)
- `vitalia/docs/archive/2026/stories/vitalia-fase1-sub-tabs-line2/03-arch.md` (SubTabsBar 22-subtab pattern)

**Core greps (anti-duplication audit):**
- `core/luana-core-iam/src/luana_core_iam/api/routers/auth_router.py` (endpoint canonical)
- `nicolify/backend/src/main.py` (reference mount pattern line 544)
- `vitalia/frontend/src/lib/` (FE helpers audit)
- Cross-brand grep {nicolify,comunify,lupulo}/frontend/src/ (mirror scan)

**Rules & overlay:**
- `.claude/rules/` (11 universal rules)
- `vitalia/.claude/rules/hipaa-lite.md`, `vitalia/.claude/rules/shell-mockup-per-component.md` (vitalia overlay)

## 13. Verbatim grep + WebFetch commands executed

**Grep audit (anti-duplication scan) — all verbatim:**

```bash
# 1. Core endpoint existence
grep -rn "me/tenants\|get_my_tenants" /home/chalreme/Proyectos/luana-vitalia/core/luana-core-iam/src/
# Result: core/luana-core-iam/src/luana_core_iam/api/routers/auth_router.py:23:@router.get("/me/tenants")
# Result: core/luana-core-iam/src/luana_core_iam/api/routers/auth_router.py:24:async def get_my_tenants(

# 2. Nicolify mount pattern
grep -n "from luana_core_iam\|auth_router" /home/chalreme/Proyectos/luana-vitalia/nicolify/backend/src/main.py
# Result: line 92 `from luana_core_iam.api.routers import auth_router as iam_users`
# Result: line 544 `prefix="/api/v1/iam/users"` (include_router call)

# 3. Vitalia legacy consumer audit
grep -rn "/api/v1/iam/me" /home/chalreme/Proyectos/luana-vitalia/vitalia/frontend/src/ /home/chalreme/Proyectos/luana-vitalia/vitalia/backend/tests/
# Result: DashboardWelcome.tsx (DELETED T-5) + test (DELETED) → zero consumers post-cleanup

# 4. Vitalia FE helper audit
grep -rn "fetchUserTenants\|getUserTenants\|/me/tenants" /home/chalreme/Proyectos/luana-vitalia/vitalia/frontend/src/
# Result: zero matches → NEW file lib/iam/api.ts

# 5. proxy.ts existence
ls -la /home/chalreme/Proyectos/luana-vitalia/vitalia/frontend/src/proxy.ts /home/chalreme/Proyectos/luana-vitalia/vitalia/frontend/src/middleware.ts
# Result: proxy.ts EXISTS (F1-S0), middleware.ts NOT FOUND

# 6. Cross-brand mirror scan
for B in nicolify comunify lupulo; do grep -rln "proxy.ts\|fetchUserTenants" /home/chalreme/Proyectos/luana-vitalia/$B/frontend/src/ 2>/dev/null; done
# Result: zero matches → brand-local pattern
```

**WebFetch audit:** SKIPPED (architect phase complete; builder inherits ratified decisions, not reasoning. Framework URLs documented in § 15 for reference.)

**Grep execution count:** 6 multiline greps + 1 ls check (7 total cross-codebase checks)

## 14. Free-form notes — what I'd tell a smart colleague

This is a straightforward routing + legacy-cleanup story with architect ratificación complete. Three gotchas to watch:

**#1 — Anti-duplication enforcement:** BE wiring is a 1-line mount of core `auth_router` (parità nicolify:544 verbatim). DO NOT create a vitalia-local `/me/tenants` endpoint. The vitalia stub `/me` (Slice 1 legacy) must be deleted atomically; zero consumers remain post-T-5 cleanup (DashboardWelcome is deleted anyway). Architect explicitly marked "anti-duplication — deleted vitalia local /me stub" in 03-arch-be.md § 1.

**#2 — Legacy cleanup scope-creep:** T-5 deletes entire `(dashboard)/` + `(app)/` route groups. Builder must grep `features/dashboard/` to confirm zero remaining consumers before deleting. Architect notes "if zero → delete feature dir entirely; if non-zero (unlikely) → leave feature, only delete route group." This decision blocks F1-S10 (empty-states) which needs the routing tree operational.

**#3 — Server-First boundary:** Only 2 of the 5 new routing pages are Client Components (`[agent]/not-found.tsx` for `useParams()`, `RefreshButton.tsx` for `useRouter().refresh()`). Architect enforces this explicitly; allowlist `test_server_first.test.ts` must shrink (remove obsolete allowlisted files) and stay green.

**Cross-cutting patterns:** Network-fallback is humble (no auto-retry; "Reintentar" button calls `refresh()` client-side — F1-S9 scope, BE audit endpoint for F2 Fase 2). HIPAA-lite audit row captures userId + attemptedTenant + timestamp (no PHI; `/me/tenants` is identity+tenant list, exempt from dual-filter). Default landing changed to Valeria/Agenda (user mental model shift).

**E2E coverage:** 8 Gherkin scenarios (100% coverage per 04-validators.yaml). Playwright visual goldens for not-found.tsx outer + inner + network-error per dark/light mode. Accessibility test (axe-core + keyboard). i18n test (Spanish neutro per glosario). No Opus in build (FE+BE 100% Sonnet/opencode).

**Predecessor cites:** Shell was built atomically F1-S0..S8; F1-S9 is plumbing. F1-S4 set layout + landing redirect (F1-S9 changes target). F1-S7 built Ribbon 6-tab stub `[...slug]` catchall (F1-S9 deletes, replaces with typed pages). F1-S8 built SubTabsBar 22-subtab pattern (F1-S9 routes each to placeholder page). After merge, shell is navigable end-to-end (empty content — F1-S10 fills in).

## 15. Upstream canonical docs references

**Cited in architect artifacts (ratified, no re-verification needed for builder):**

| Framework | Canonical URL | Access date | Key section |
|---|---|---|---|
| Next.js 16 `proxy` file | https://nextjs.org/docs/app/api-reference/file-conventions/proxy | 2026-05-26 (architect) | Function exported as `proxy` (not `middleware`); Node.js runtime default; negative matcher; `auth.protect()` auto-redirect `/sign-in` |
| Clerk `@clerk/nextjs` middleware | https://clerk.com/docs/reference/nextjs/clerk-middleware | 2026-05-26 (architect) | `clerkMiddleware()` integration, `createRouteMatcher()` pattern, `auth.protect()` |
| React 19 Server Components | (via react patterns skill) | 2026-05-26 (architect) | `async function` pages, `params: Promise<>` async pattern (Next.js 16 update) |
| Shadcn Button component | https://ui.shadcn.com/docs/components/button | implicit (lib reused) | `asChild` prop + Link composition (not-found.tsx) |
| Tailwind v4 semantic tokens | https://tailwindcss.com/docs | implicit (design tokens F1-S1) | Vitalia token schema (--background, --agent-*, opacity-*, text-muted-foreground) |

**Decision:** Framework URLs captured by architect; builder inherits ratified solutions (no new research needed). Programmer can reference URLs if troubleshooting Next.js 16-specific patterns (`params: Promise`, `proxy` vs `middleware` deprecation).

## 16. Self-budget snapshot

**Execution summary:**
- Turns used: 18 (init audit + skeleton + parallel reads × 3 + bash greps + edits × 14 brief sections + final audit)
- Tools called: Read (8), Bash (5), Edit (15), Write (2)
- Files read: 8 story files (primary) + 2 predecessor reference files + 2 rules + audit log review
- Total bytes processed: ~110k
- Greps executed: 6 cross-codebase multiline checks (anti-duplication audit)
- WebFetch attempts: 0 (architect phase complete; builder inherits ratified)
- Time wall-clock: ~15 min (parallel reads + audit log + sequential brief section edits)

**Token estimate (rough 1 char = 1/3.5 token):**
- Input context (rules + MEMORY + instructions): ~65k tokens
- Output artifacts (CONTEXT-BRIEF.md + audit log): ~8k tokens  
- Haiku 4.5 reasoning chains: ~4k tokens
- **Total estimated:** ~77k tokens (well within budget; 200k ceiling)

**Turns remaining:** ~82 turns (conservative; actual usage lower due to Haiku efficiency)
